// básica (existe undefined e null também)
const nome: string = "Lorenzo";

// array
const listaNomes: string[] = ["Lorenzo", "Hannah"];

//objeto
const usuario: { nome: string; idade: number; ativo: boolean } = {
    nome: "Nara",
    idade: 20,
    ativo: true,
};

// é recomendado isso nunca existir
const qualquerCoisa: any = "Lorenzo";
